# Local development: keep requests same-origin through the Vite dev proxy,
# which forwards /api/v1 to http://localhost:8080 (see vite.config.ts).
# Do not set an absolute URL here: the backend has no CORS configuration,
# so direct cross-origin calls would be blocked by the browser.
VITE_API_BASE_URL=/api/v1
